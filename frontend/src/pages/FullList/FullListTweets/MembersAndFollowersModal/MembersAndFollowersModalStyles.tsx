import { makeStyles } from "@material-ui/core";

export const useMembersAndFollowersModalStyles = makeStyles(() => ({
    content: {
        height: 577,
        width: 598,
        padding: 0,
        overflowX: "hidden"
    }
}));
