import { makeStyles } from "@material-ui/core";

export const useEmailVerificationModalStyles = makeStyles(() => ({
    emailLinkWrapper: {
        display: "block",
        marginLeft: 10,
        marginTop: 2
    }
}));
