import { makeStyles } from "@material-ui/core";

export const useSetPasswordModalStyles = makeStyles(() => ({
    title: {
        marginTop: 20
    },
    subtitle: {
        marginTop: 15
    },
    controllerWrapper: {
        marginTop: 10
    }
}));
