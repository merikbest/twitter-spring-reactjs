import { makeStyles, Theme } from "@material-ui/core";

export const useChatUserBlockedStyles = makeStyles((theme: Theme) => ({
    blockedInfoText: {
        textAlign: "center",
        height: 30
    }
}));
