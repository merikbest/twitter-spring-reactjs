import { makeStyles } from "@material-ui/core";

export const useMessageSettingsStyles = makeStyles(() => ({
    chatContainer: {
        minWidth: 600,
        padding: 0,
        borderLeft: 0
    },
    chatHeader: {
        width: 598
    }
}));
