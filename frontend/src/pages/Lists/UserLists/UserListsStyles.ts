import { makeStyles, Theme } from "@material-ui/core";

export const useUserListsStyles = makeStyles((theme: Theme) => ({
    myLists: {
        height: 316,
        border: 0,
        borderRadius: 0
    }
}));
