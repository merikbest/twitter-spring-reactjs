import { makeStyles, Theme } from "@material-ui/core";

export const useRemoveSearchResultButtonStyles = makeStyles((theme: Theme) => ({
    closeIconButton: {
        marginLeft: "auto",
        float: "right"
    }
}));
