import { makeStyles } from "@material-ui/core";

export const useSettingsModalStyles = makeStyles(() => ({
    content: {
        height: 550,
        width: 598,
        padding: 0,
        overflowX: "hidden"
    }
}));
